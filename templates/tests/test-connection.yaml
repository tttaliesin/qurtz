apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qurtz.fullname" . }}-test-connection"
  labels:
    {{- include "qurtz.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "qurtz.fullname" . }}-web:{{ .Values.service.port }}']
  restartPolicy: Never
